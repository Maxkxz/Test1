#pragma once
#ifndef MY_ADD_HPP
#define MY_ADD_HPP

/**
* @brief The function which adds two numbers
* @param [in] a - the first number
* @param [in] b - the second number
* @return the sum of a and b
* @warning int only
*/

int my_add(int, int);

#endif
